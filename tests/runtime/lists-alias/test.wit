package my:lists;

world runner {
  import cat: interface {
    type my-list = list<u8>;
    foo: func(x: my-list);
    bar: func() -> my-list;
  }

  export run: func();
}

world test {
  export cat: interface {
    type my-list = list<u8>;
    foo: func(x: my-list);
    bar: func() -> my-list;
  }
}
